// Config de la demo pública (/demo).
// Completar cuando se cree el consultorio y la cuenta demo en Supabase.

// Slug público del consultorio demo (ej: "consultorio-demo"). Vacío = bloque
// "Web pública" deshabilitado ("Próximamente").
export const DEMO_CLINIC_SLUG = "";

// true cuando exista la cuenta demo + el mecanismo de login para mostrar el
// panel real. false = bloque "Tu agenda en el panel" deshabilitado.
export const DEMO_PANEL_ENABLED = false;

// Ruta del portal del paciente demo (ya existe y funciona sin login).
export const DEMO_PORTAL_PATH = "/portal-paciente/demo";
